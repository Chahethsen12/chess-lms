[build]
builder = "nixpacks"
buildCommand = "cd ../.. && pnpm install && pnpm turbo build --filter=@chess-lms/api"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
